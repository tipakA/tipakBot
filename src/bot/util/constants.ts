import { en } from './localization/en';
import { pl } from './localization/pl';

export const dialog = {
  en: en.dialog,
  pl: pl.dialog,
};

export const error = {
  en: en.errors,
  pl: pl.errors,
};

export const permission = {
  en: en.permissions,
  pl: pl.permissions,
};